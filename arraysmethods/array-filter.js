// Creates a new array with only the elements that pass a test//
//It doesnt change the oriinal array//


//keep only the even number

let number = [1,2,3,4];

let evenNumber = number.filter (num = num % 2 === 0);

console.log(evenNumbers);